<h4>Hello <strong><em>{{ signInForm.vars.value.email }}</em></strong> ,</h4>

<p>This is part of changing password process if you forgot it.
   This letter contains new automatically generated password that you can use on login page.
   Using link below you will redirected to sign in form where you must fill your <strong><em>email</em></strong>
   and <strong><em>new password</em></strong> in appropriate fields of sign in form and submit them.
</p>

<a href="{{ absolute_url(path('login_signin')) }}">Click this link and you will be redirected on sign in form.</a>

<dl>
    <dt><dfn>New password:</dfn></dt>
    <dd><strong>{{ signInForm.vars.value.pass }}</strong></dd>
</dl>

    <p>Or if your messanger supports, just click <strong><em>Sign in</em></strong> button</p>
{{ form_start(signInForm, { 'action': absolute_url(path('login_signin')) } ) }}
    <input type="hidden" name="{% if signInForm.vars.full_name %}{{ signInForm.vars.full_name }}[email]{% else %}email{% endif %}"  value="{{ signInForm.vars.value.email }}">
    <input type="hidden" name="{% if signInForm.vars.full_name %}{{ signInForm.vars.full_name }}[pass]{% else %}pass{% endif %}"  value="{{ signInForm.vars.value.pass }}">
    <input type="hidden" name="{% if signInForm.vars.full_name %}{{ signInForm.vars.full_name }}[_remember_me]{% else %}_remember_me{% endif %}" value="false">
    {{ form_widget(signInForm._token) }}
    <button type="submit">Sign in</button>
{{ form_end(signInForm, {'render_rest': false}) }}
    
    
<p>This message was sent to you because you or someone other pointed this email address in "forgot password" form.</p>
<strong><em>if you didn't do it then You mustn't do any actions, but remember that current password was changed and is pointed above.</em></strong>